{% extends 'base.html.twig' %}

{% block title %}Loans{% endblock %}

{% block body %}
    <h1>Loans</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ loan.id }}</td>
            </tr>
            <tr>
                <th>Amount</th>
                <td>{{ loan.amount }}</td>
            </tr>
            <tr>
                <th>CreaionDate</th>
                <td>{{ loan.creaionDate ? loan.creaionDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Percent</th>
                <td>{{ loan.percent }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_loans_index') }}">back to list</a>

    <a href="{{ path('app_loans_edit', {'id': loan.id}) }}">edit</a>

    {{ include('loans/_delete_form.html.twig') }}
{% endblock %}
